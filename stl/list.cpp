#include <bits/stdc++.h>
using namespace std;

int main(){
    list<int> ls;

    ls.push_back(2);
    ls.emplace_back(10);

    ls.push_front(3);
    ls.emplace_front();

    #baki all same as vector
    
}